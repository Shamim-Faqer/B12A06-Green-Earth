//let cart = [];


/* const displayCart = (cartItem) => {
  const cartDiv = document.getElementById("cartDiv");
  cartItem.forEach((item)=>{
    const cartEle = document.createElement("div");
    cartEle.innerHTML = `<span>${item.name}</span>
          <span>$500</span>`;
    cartDiv.appendChild(cartEle);
  });
  
};
 */

